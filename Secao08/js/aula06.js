let caixaElement = document.querySelector('.caixa');

caixaElement.style.width = '200px';
caixaElement.style.height = '200px';
caixaElement.style.background = 'green';

/*
As configuraçnoes de estilo via JavaScript seguem os mesmo 
conceitos e atributos utilizados via CSS.

OBS: Diferença

CSS -- onde usa tivermos background-color
JS  -- usaremos a seguinte determinação backgroundColor (junta-se a palavra e a palavra separada a primeira letra vira maiúscula)

Outro exemplo:

CSS -- fonte-size;
JS  -- fonteSize;
*/